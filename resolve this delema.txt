new myFGUIWidget;

- calls constructor
	- if (parent) parent->attach_widget(this);
	- (increment static total num widgets)

	
delete myFGUIWidget;

- calls the destructor
	- deletes the collision area
	- if (parent) parent->detach_widget(this);
	- (decrements the static total num of widgets)
	
	
	
====

new myFGUIParent;

- calls the constructor
	- calls the FGUIWidget constructor
	

delete myFGUIParent

- if (parent) parent->detach_widget(this);
	- delete collision_area
	- foreach(child) { delete child; i--; }


